#pragma once
#include<vector>
using namespace std;

/*
class Map
{
    private:
        vector<Node&> NodeList;
        vector<Edge&> EdgeList;
    public:
        Map();
        Map(vector<Node&> _NodeList,vector<Edge&> EdgeList);
        void addNode(Node& newNode);
        void addEdge(Edge& newEdge);
        vector<Node&> getNodeList() const;
        vector<Edge&> getEdgeList() const;
        //How to ensure Edge only contains nodes from map
};
*/